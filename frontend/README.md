# PixelHub Frontend

Веб-приложение для интерактивного Pixel-Art полотна, построенное на Angular 20. Позволяет пользователям совместно создавать пиксельное искусство в реальном времени через WebSocket соединение.

## 📋 Содержание

- [Требования](#требования)
- [Установка](#установка)
- [Запуск проекта](#запуск-проекта)
- [Переменные окружения](#переменные-окружения)
- [Сборка для продакшена](#сборка-для-продакшена)
- [Структура проекта](#структура-проекта)
- [Основные компоненты](#основные-компоненты)
- [Разработка](#разработка)

## 🔧 Требования

Перед началом работы убедитесь, что у вас установлены:

- **Node.js** версии 18.x или выше
- **npm** версии 9.x или выше (обычно устанавливается вместе с Node.js)

Проверить версии можно командами:
```bash
node --version
npm --version
```

## 📦 Установка

1. Перейдите в директорию проекта:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

После установки все необходимые пакеты будут загружены в папку `node_modules/`.

## 🚀 Запуск проекта

### Режим разработки

Для запуска локального сервера разработки выполните:

```bash
npm run start
```

Приложение будет доступно по адресу `http://localhost:4200/`. Приложение автоматически перезагружается при изменении исходных файлов.

**Важно:** Перед запуском автоматически выполняется скрипт `scripts/generate-env.js`, который генерирует файл `src/environments/environment.ts` на основе переменных окружения.

### Остановка сервера

Для остановки сервера разработки нажмите `Ctrl+C` в терминале.

## 🔐 Переменные окружения

Проект использует переменные окружения для настройки основных параметров. Перед запуском или сборкой можно задать следующие переменные:

### Доступные переменные

| Переменная | Описание | Значение по умолчанию |
|------------|----------|------------------------|
| `NG_APP_API_BASE` | Базовый URL API бэкенда | `http://localhost:8080` |
| `NG_APP_CANVAS_WIDTH` | Ширина канваса в пикселях | `2000` |
| `NG_APP_CANVAS_HEIGHT` | Высота канваса в пикселях | `2000` |
| `NG_APP_RATE_LIMIT_SECONDS` | Задержка между размещением пикселей (в секундах) | `1` |

### Как передать переменные окружения

#### macOS / Linux (bash, zsh)

```bash
NG_APP_API_BASE=https://api.example.com \
NG_APP_CANVAS_WIDTH=3000 \
NG_APP_CANVAS_HEIGHT=3000 \
NG_APP_RATE_LIMIT_SECONDS=2 \
npm run start
```

#### Windows PowerShell

```powershell
$env:NG_APP_API_BASE = "https://api.example.com"
$env:NG_APP_CANVAS_WIDTH = "3000"
$env:NG_APP_CANVAS_HEIGHT = "3000"
$env:NG_APP_RATE_LIMIT_SECONDS = "2"
npm run start
```

#### Windows CMD

```cmd
set NG_APP_API_BASE=https://api.example.com && set NG_APP_CANVAS_WIDTH=3000 && set NG_APP_CANVAS_HEIGHT=3000 && set NG_APP_RATE_LIMIT_SECONDS=2 && npm run start
```

### Как это работает

1. При запуске `npm run start` автоматически выполняется скрипт `prestart`, который запускает `scripts/generate-env.js`
2. Скрипт читает переменные окружения из процесса Node.js
3. Генерируется файл `src/environments/environment.ts` с актуальными значениями
4. Приложение использует эти значения во время выполнения

**Примечание:** Если переменные окружения не заданы, используются значения по умолчанию.

## 🏗️ Сборка для продакшена

Для создания production-сборки выполните:

```bash
npm run build
```

Сборка будет создана в директории `dist/frontend/browser/`. Файлы оптимизированы для продакшена (минификация, tree-shaking и т.д.).

**Важно:** Перед сборкой также автоматически выполняется скрипт `prebuild`, который генерирует `environment.ts` на основе переменных окружения.

### Сборка с переменными окружения

```bash
NG_APP_API_BASE=https://api.production.com \
NG_APP_CANVAS_WIDTH=2000 \
NG_APP_CANVAS_HEIGHT=2000 \
NG_APP_RATE_LIMIT_SECONDS=1 \
npm run build
```

### Конфигурации сборки

Проект поддерживает две конфигурации:

- **development** (по умолчанию для `ng serve`) — с source maps, без оптимизации
- **production** (по умолчанию для `ng build`) — с оптимизацией и минификацией

Для явного указания конфигурации:

```bash
ng build --configuration development
ng build --configuration production
```

## 📁 Структура проекта

```
frontend/
├── scripts/
│   └── generate-env.js          # Скрипт генерации environment.ts
├── src/
│   ├── app/
│   │   ├── components/          # Компоненты приложения
│   │   │   ├── auth-dialog/     # Диалог авторизации
│   │   │   ├── canvas-board/    # Основной компонент канваса
│   │   │   ├── sidebar/         # Боковая панель (лидерборд, статусы)
│   │   │   └── toolbar/         # Панель инструментов (палитра цветов)
│   │   ├── core/
│   │   │   ├── constants/       # Константы приложения
│   │   │   ├── models/          # Модели данных
│   │   │   └── services/        # Сервисы (логика приложения)
│   │   ├── app.ts               # Корневой компонент
│   │   ├── app.html             # Шаблон корневого компонента
│   │   └── app.scss             # Стили корневого компонента
│   ├── environments/
│   │   ├── environment.ts       # Автогенерируемый файл окружения
│   │   ├── environment.development.ts
│   │   └── environment.prod.ts
│   ├── index.html               # Главный HTML файл
│   ├── main.ts                  # Точка входа приложения
│   ├── polyfills.ts            # Полифиллы
│   └── styles.scss              # Глобальные стили
├── angular.json                 # Конфигурация Angular CLI
├── package.json                 # Зависимости и скрипты
└── tsconfig.json                # Конфигурация TypeScript
```

## 🧩 Основные компоненты

### Canvas Board (`canvas-board`)
Основной компонент для отображения и взаимодействия с пиксельным полотном:
- Рендеринг канваса с поддержкой масштабирования и панорамирования
- Размещение пикселей по клику
- Подсветка пикселя под курсором
- Навигация по координатам

### Toolbar (`toolbar`)
Панель инструментов с палитрой цветов для выбора цвета пикселей.

### Sidebar (`sidebar`)
Боковая панель, отображающая:
- Лидерборд активных пользователей
- Статусные сообщения приложения

### Auth Dialog (`auth-dialog`)
Диалоговое окно для авторизации пользователя по никнейму.

## 🔌 Основные сервисы

- **`AuthService`** — управление аутентификацией и JWT токенами
- **`RealtimeService`** — WebSocket соединение для real-time обновлений
- **`CanvasStateService`** — состояние канваса и пикселей
- **`PixelPlacementService`** — логика размещения пикселей с учетом кулдауна
- **`CooldownService`** — управление задержкой между действиями
- **`LeaderboardService`** — получение данных лидерборда
- **`BoardLoaderService`** — загрузка полного состояния доски

## 🛠️ Разработка

### Доступные команды

```bash
# Запуск сервера разработки
npm run start

# Сборка проекта
npm run build

# Сборка в режиме watch (автоматическая пересборка при изменениях)
npm run watch

# Запуск тестов
npm test

# Генерация environment.ts вручную
node scripts/generate-env.js
```

### Генерация компонентов

Angular CLI предоставляет инструменты для генерации кода:

```bash
# Генерация компонента
ng generate component component-name

# Генерация сервиса
ng generate service service-name

# Полный список доступных генераторов
ng generate --help
```

### Стилизация

Проект использует SCSS для стилей. Глобальные стили находятся в `src/styles.scss`, стили компонентов — в соответствующих `.component.scss` файлах.

### Тестирование

Для запуска unit-тестов используется Karma:

```bash
npm test
```

## 📝 Дополнительная информация

- **Angular CLI**: [Документация](https://angular.dev/tools/cli)
- **Angular**: [Официальная документация](https://angular.dev)
- **TypeScript**: [Документация](https://www.typescriptlang.org/docs/)

## ⚠️ Важные замечания

1. **Переменные окружения**: Всегда перезапускайте сервер разработки после изменения переменных окружения, так как они применяются только при старте приложения.

2. **Файл environment.ts**: Файл `src/environments/environment.ts` автоматически генерируется скриптом и не должен редактироваться вручную. Все изменения будут перезаписаны при следующем запуске.

3. **Бэкенд**: Убедитесь, что бэкенд сервер запущен и доступен по адресу, указанному в `NG_APP_API_BASE`.

4. **WebSocket**: Приложение требует WebSocket соединение для работы в реальном времени. Убедитесь, что бэкенд поддерживает WebSocket (STOMP over SockJS).
