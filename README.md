# PixelHub

PixelHub — это интерактивная веб-платформа для совместного рисования пикселей в реальном времени. Пользователи могут размещать пиксели на общем холсте, просматривать историю изменений и соревноваться в таблице лидеров.

Ссылка на презентацию: https://docs.google.com/presentation/d/1feHDH8gHgP3MgT3YaanjfBT_aBjMCp5D0NnrMoPyo0o/edit?usp=sharing

## 🎨 Возможности

### Основной функционал

- **Интерактивный холст**: Совместное рисование пикселей на общем холсте размером 2000×2000 пикселей
- **Реальное время**: Обновления холста синхронизируются между всеми пользователями через WebSocket
- **История изменений**: Просмотр состояния холста в любой момент времени
- **Таблица лидеров**: Рейтинг пользователей по количеству размещенных пикселей
- **Система кулдауна**: Ограничение частоты размещения пикселей для баланса игры
- **Аутентификация**: Простая система входа по никнейму с JWT токенами

### Интерфейс

- **Масштабирование**: Зум от 0.25× до 40× с помощью колесика мыши или слайдера
- **Перемещение**: Перетаскивание холста мышью для навигации
- **Выбор цвета**: Палитра из 128 цветов (0-127)
- **Поиск координат**: Переход к конкретным координатам на холсте
- **Адаптивный дизайн**: Поддержка мобильных устройств и десктопов

## 🛠 Технологии

### Backend
- **Java 21** с Spring Boot 3.5.7
- **PostgreSQL** для хранения данных
- **WebSocket (STOMP)** для реального времени
- **JWT** для аутентификации
- **Liquibase** для миграций базы данных
- **Spring Data JPA** для работы с БД

### Frontend
- **Angular 20** с TypeScript
- **RxJS** для реактивного программирования
- **STOMP.js** для WebSocket соединений
- **Canvas API** для рендеринга пикселей
- **Nginx** для статической раздачи в продакшене

### Инфраструктура
- **Docker** и **Docker Compose** для контейнеризации
- **Gradle** для сборки бэкенда
- **npm** для управления зависимостями фронтенда

## 📋 Требования

- Docker и Docker Compose
- Java 21 (для локальной разработки бэкенда)
- Node.js 22+ (для локальной разработки фронтенда)

## 🚀 Быстрый старт

### Запуск через Docker Compose

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd PixelHub
```

2. Создайте файл `.env` в корне проекта:
```env
POSTGRES_USER=pixelhub
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=pixelhub
JWT_SECRET=your_jwt_secret_key_min_64_chars
RATE_LIMIT=30
CANVAS_WIDTH=2000
CANVAS_HEIGHT=2000
```

3. Запустите проект:
```bash
docker-compose up -d
```

4. Откройте браузер:
   - Frontend: http://localhost:8081
   - Backend API: http://localhost:8080

### Локальная разработка

#### Backend

1. Убедитесь, что PostgreSQL запущен и доступен
2. Настройте `application.properties` с параметрами подключения к БД
3. Соберите проект:
```bash
cd backend
./gradlew build
```
4. Запустите приложение:
```bash
./gradlew bootRun
```

#### Frontend

1. Установите зависимости:
```bash
cd frontend
npm install
```

2. Запустите dev-сервер:
```bash
npm start
```

3. Откройте http://localhost:4200

## 🎮 Как использовать

### Первый запуск

1. Откройте приложение в браузере
2. Введите уникальный никнейм в диалоге авторизации
3. Начните рисовать!

### Размещение пикселей

1. Выберите цвет из палитры
2. Кликните на холст в нужном месте
3. Пиксель будет размещен (если не действует кулдаун)

### Навигация по холсту

- **Зум**: Используйте колесико мыши или слайдер зума
- **Перемещение**: Зажмите левую кнопку мыши и перетащите холст
- **Центрирование**: Нажмите кнопку "Center" для возврата к центру
- **Поиск координат**: Введите координаты X и Y в поле "Go to" и нажмите Enter

### Просмотр истории

1. Откройте панель истории
2. Выберите дату и время
3. Холст отобразит состояние на выбранный момент

### Таблица лидеров

Откройте боковую панель, чтобы увидеть топ-10 пользователей по количеству размещенных пикселей.

## ⚙️ Конфигурация

### Переменные окружения

#### Backend (`application.properties` или переменные окружения)

- `SPRING_DATASOURCE_URL` - URL подключения к PostgreSQL
- `SPRING_DATASOURCE_USERNAME` - Имя пользователя БД
- `SPRING_DATASOURCE_PASSWORD` - Пароль БД
- `JWT_SECRET` - Секретный ключ для JWT (минимум 64 символа)
- `RATE_LIMIT` - Кулдаун между размещениями пикселей (в секундах)
- `CANVAS_WIDTH` - Ширина холста в пикселях
- `CANVAS_HEIGHT` - Высота холста в пикселях
- `MIN_COLOR` - Минимальное значение цвета (по умолчанию 0)
- `MAX_COLOR` - Максимальное значение цвета (по умолчанию 127)

#### Frontend (build arguments)

- `NG_APP_API_BASE` - Базовый URL API (по умолчанию `/api`)
- `NG_APP_CANVAS_WIDTH` - Ширина холста
- `NG_APP_CANVAS_HEIGHT` - Высота холста
- `NG_APP_RATE_LIMIT_SECONDS` - Кулдаун в секундах

## 📡 API Endpoints

### REST API

- `GET /full-board` - Получить текущее состояние всего холста
- `GET /board-history?timestamp=<ISO_DATE_TIME>` - Получить состояние холста на момент времени
- `GET /leaderboard` - Получить таблицу лидеров
- `POST /token` - Создать новый токен для никнейма
- `POST /token-refresh` - Обновить существующий токен

### WebSocket

- **Подключение**: `/ws`
- **Отправка пикселя**: `/app/pixel` (требуется аутентификация)
- **Получение обновлений**: `/topic/pixels`

## 🗄 Структура базы данных

### Таблица `pixels`
- `id` - Уникальный идентификатор
- `x` - Координата X
- `y` - Координата Y
- `color` - Значение цвета (0-127)
- `placed_at` - Время размещения

### Таблица `users`
- `id` - Уникальный идентификатор
- `nickname` - Уникальный никнейм пользователя
- `pixel_count` - Количество размещенных пикселей
- `last_placed_at` - Время последнего размещения пикселя

## 🔒 Безопасность

- JWT токены для аутентификации
- Валидация координат и цветов на сервере
- Rate limiting для предотвращения спама
- CORS настройки для защиты API
- WebSocket аутентификация через токены
